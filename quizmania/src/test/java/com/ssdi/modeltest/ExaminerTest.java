package com.ssdi.modeltest;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;

import com.ssdi.model.Admin;
import com.ssdi.model.Examiner;

import mockit.integration.junit4.JMockit;

@RunWith(JMockit.class)
@SpringBootTest
public class ExaminerTest {
	

/*	@Tested
	
	
	@Test
	 public void examinerCreation() {
	 
		Examiner testExaminer = new Examiner("TestUserFirstName", "TestUserLastName", "TestAdminUserEmail", "TestUserPassword");
		
	 new Verifications() {{
	 assertEquals(null, test_building.getName());
	 }};
	
	 // When: set name is invoked
	 test_building.setName("Test Building");
	
	 // Then: provided name is set correctly
	 new Verifications() {{
	 assertEquals("Test Building", test_building.getName());
	 }};*/
	 }

